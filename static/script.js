const chatMessages=document.getElementById('chatMessages');const composerForm=document.getElementById('composerForm');const textInput=document.getElementById('textInput');const videoInput=document.getElementById('videoInput');const attachBtn=document.getElementById('attachBtn');const fileChip=document.getElementById('fileChip');const sendBtn=document.getElementById('sendBtn');const voiceBtn=document.getElementById('voiceBtn');const newChatBtn=document.getElementById('newChatBtn');const liveScreenBtn=document.getElementById('liveScreenBtn');const stopLiveScreenBtn=document.getElementById('stopLiveScreenBtn');const liveScreenPanel=document.getElementById('liveScreenPanel');const liveScreenPreview=document.getElementById('liveScreenPreview');const liveScreenStatus=document.getElementById('liveScreenStatus');const liveStatus=document.getElementById('liveStatus');let selectedFile=null,screenStream=null,liveCanvas=null,liveContext=null,liveFrameTimer=null,liveConnected=false,latestFrameData='';let recognition=null,isListening=false;const SpeechRecognition=window.SpeechRecognition||window.webkitSpeechRecognition;
if(SpeechRecognition){recognition=new SpeechRecognition();recognition.lang='hi-IN';recognition.continuous=false;recognition.interimResults=true;recognition.onstart=()=>{isListening=true;voiceBtn.classList.add('listening');voiceBtn.textContent='⏹️';textInput.placeholder='Bol rahe ho...'};recognition.onresult=e=>{let t='';for(let i=e.resultIndex;i<e.results.length;i++)t+=e.results[i][0].transcript;textInput.value=t;textInput.dispatchEvent(new Event('input'))};recognition.onerror=()=>stopVoiceInput();recognition.onend=()=>stopVoiceInput()}function stopVoiceInput(){isListening=false;voiceBtn.classList.remove('listening');voiceBtn.textContent='🎙️';textInput.placeholder='Message likho ya 🎙️ dabakar bolo...'}voiceBtn.addEventListener('click',()=>{if(!recognition){addBotBubble('⚠️ Is browser me voice input supported nahi hai. Chrome use karo.');return}if(isListening)recognition.stop();else try{recognition.start()}catch(e){}});
attachBtn.addEventListener('click',()=>videoInput.click());videoInput.addEventListener('change',()=>{if(videoInput.files.length){selectedFile=videoInput.files[0];fileChip.textContent='📎 '+selectedFile.name;fileChip.classList.remove('hidden')}});newChatBtn.addEventListener('click',()=>{stopLiveScreen();stopVoiceInput();chatMessages.innerHTML='<div class="msg msg-bot"><div class="bubble">👋 Video bhejo aur batao kya karna hai. Ek hi bubble me text ya 🎙️ voice dono se baat karo.</div></div>';selectedFile=null;fileChip.classList.add('hidden');textInput.value=''});textInput.addEventListener('input',()=>{textInput.style.height='auto';textInput.style.height=Math.min(textInput.scrollHeight,100)+'px'});function scrollToBottom(){chatMessages.scrollTop=chatMessages.scrollHeight}function escapeHtml(t){const d=document.createElement('div');d.textContent=t;return d.innerHTML}function addUserMessage(t,f){const d=document.createElement('div');d.className='msg msg-user';d.innerHTML=`<div class="bubble">${f?`<div class="video-chip-inline">📎 ${escapeHtml(f)}</div><br>`:''}${escapeHtml(t)}</div>`;chatMessages.appendChild(d);scrollToBottom()}function addBotBubble(c,x=''){const d=document.createElement('div');d.className='msg msg-bot';d.innerHTML=`<div class="bubble ${x}">${c}</div>`;chatMessages.appendChild(d);scrollToBottom();return d}function segmentsToTable(s){return `<div>${s.length} segments mile:</div><table class="seg-table"><thead><tr><th>Start</th><th>End</th><th>Label</th><th>Reason</th></tr></thead><tbody>${s.map(x=>`<tr><td>${x.start_fmt}</td><td>${x.end_fmt}</td><td>${escapeHtml(x.label||'')}</td><td>${escapeHtml(x.reason||'')}</td></tr>`).join('')}</tbody></table>`}
composerForm.addEventListener('submit',async e=>{e.preventDefault();if(isListening&&recognition)recognition.stop();const i=textInput.value.trim();if(liveConnected){if(!i)return;addUserMessage(i);textInput.value='';textInput.style.height='auto';await sendLiveText(i);return}if(!selectedFile)return addBotBubble('⚠️ Pehle video attach karo (📎 icon dabao).','error');if(!i)return addBotBubble('⚠️ Instructions likho ya 🎙️ se bolo.','error');addUserMessage(i,selectedFile.name);const f=new FormData;f.append('video',selectedFile);f.append('instructions',i);textInput.value='';textInput.style.height='auto';sendBtn.disabled=true;const l=addBotBubble('⏳ Video analyze ho raha hai, thoda time lagega...','loading');try{const r=await fetch('/analyze',{method:'POST',body:f}),d=await r.json();if(!r.ok)throw Error(d.error||'Kuch galat ho gaya.');l.remove();addBotBubble(segmentsToTable(d.segments))}catch(err){l.remove();addBotBubble('❌ '+err.message,'error')}finally{sendBtn.disabled=false;selectedFile=null;fileChip.classList.add('hidden');videoInput.value=''}});
if(liveScreenBtn)liveScreenBtn.addEventListener('click',startLiveScreen);if(stopLiveScreenBtn)stopLiveScreenBtn.addEventListener('click',stopLiveScreen);
function isMobileBrowser(){return /Android|iPhone|iPad|iPod/i.test(navigator.userAgent)}
async function startLiveScreen(){if(liveConnected)return;if(!window.isSecureContext){return addBotBubble('❌ Screen sharing ke liye HTTPS zaroori hai. Is Render URL ko HTTPS se hi open karo.','error')}if(!navigator.mediaDevices){return addBotBubble('❌ Is browser me MediaDevices API available nahi hai. Chrome ke latest version me page kholo.','error')}if(!navigator.mediaDevices.getDisplayMedia){if(isMobileBrowser()){return addBotBubble('📱 Android browser me website ko phone ki poori screen capture karne ka API available nahi hai. Ye Render URL backend ke liye bilkul sahi hai; Live Screen capture Android app ke native screen-share se chalega.','error')}return addBotBubble('❌ Is browser me screen sharing API available nahi hai. Desktop Chrome/Edge ka latest version use karo.','error')}try{liveScreenBtn.disabled=true;liveScreenBtn.textContent='⏳ Screen share start...';screenStream=await navigator.mediaDevices.getDisplayMedia({video:{frameRate:{ideal:5,max:8}},audio:false});liveScreenPreview.srcObject=screenStream;await liveScreenPreview.play().catch(()=>{});liveCanvas=document.createElement('canvas');liveContext=liveCanvas.getContext('2d',{alpha:false});liveConnected=true;liveStatus.textContent='● LIVE';liveStatus.classList.add('active');liveScreenPanel.classList.remove('hidden');liveScreenBtn.textContent='🟢 Live Active';liveScreenStatus.textContent='Live screen ready — ab chat me sawal bhejo.';addBotBubble('🟢 Live Screen connected. Ab screen ke baare me pooch sakte ho.');startLiveFrameCapture();screenStream.getVideoTracks()[0]?.addEventListener('ended',stopLiveScreen,{once:true})}catch(err){addBotBubble('❌ Live Screen start nahi hua: '+err.message,'error');cleanupLiveScreen()}}
function startLiveFrameCapture(){stopLiveFrameCapture();captureAndStoreLiveFrame();liveFrameTimer=setInterval(captureAndStoreLiveFrame,1000)}function stopLiveFrameCapture(){if(liveFrameTimer){clearInterval(liveFrameTimer);liveFrameTimer=null}}function captureAndStoreLiveFrame(){if(!liveConnected||!liveScreenPreview.videoWidth||!liveScreenPreview.videoHeight)return;const w=liveScreenPreview.videoWidth,h=liveScreenPreview.videoHeight,s=Math.min(1,1280/w);liveCanvas.width=Math.round(w*s);liveCanvas.height=Math.round(h*s);liveContext.drawImage(liveScreenPreview,0,0,liveCanvas.width,liveCanvas.height);try{latestFrameData=liveCanvas.toDataURL('image/jpeg',.65).split(',')[1]}catch(e){}}
async function sendLiveText(t){if(!liveConnected)return addBotBubble('⚠️ Live Screen active nahi hai.','error');const loading=addBotBubble('⏳ Gemini screen dekh raha hai...','loading');sendBtn.disabled=true;try{if(!latestFrameData)captureAndStoreLiveFrame();const r=await fetch('/live-chat',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({message:t,image:latestFrameData})});const d=await r.json();if(!r.ok)throw Error(d.error||'Reply nahi mila.');loading.remove();addBotBubble(d.reply)}catch(err){loading.remove();addBotBubble('❌ Reply nahi mila: '+err.message,'error')}finally{sendBtn.disabled=false}}
function stopLiveScreen(){stopLiveFrameCapture();if(screenStream){screenStream.getTracks().forEach(t=>t.stop());screenStream=null}cleanupLiveScreen()}function cleanupLiveScreen(){stopLiveFrameCapture();liveConnected=false;latestFrameData='';if(liveStatus){liveStatus.textContent='Offline';liveStatus.classList.remove('active')}if(liveScreenPanel)liveScreenPanel.classList.add('hidden');if(liveScreenBtn){liveScreenBtn.disabled=false;liveScreenBtn.textContent='🔴 Live Screen'}if(liveScreenStatus)liveScreenStatus.textContent='Screen share start nahi hua.';if(liveScreenPreview)liveScreenPreview.srcObject=null}
